export type Product = {
    id: string;
    name: string;
    value: number;
    category: string;
    price: number;
    quantity: number;
}

export type Metric = {
    key: string;
    value: string;
    icon: string;
}