export type Product = {
    id: string;
    name: string;
    value: string;
    category: string;
    price: string;
    quantity: number;
}

export type Metric = {
    key: string;
    value: string;
    icon: string;
}